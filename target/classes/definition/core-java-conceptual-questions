Please answer the below core-java Questions:

What is programing language?
A. Programming Language is a language to build the software.

What jdk contains or composed of and define each components?
A. JDK, JRE, JVM

What is IDE?
A. Integrated development environment

What are the IDEs available in the market to support java?
A. Visual studio, eclipse

Explain the entire java life cycle.
A. 1) Write it 2) Compile it 3) Run it

what is class?
A. Class is a blueprint.

What is object?
A. Is the copy of the class

What is the entry point of an application?
A. main method

Why main is static?
A. They can be create without the object.

Which class is the superclass of all classes?
A. Object class

What is difference between path and classpath variables?
A. Path is for setting path of Java tools.ClassPath it’s a path that we set for Java classes.

What is the difference between an Interface and Abstract class?
A. Interface have only method name without any body. Abstract class have implemented and unimplemented method.

Can you make an Object from an Interface and Abstract class ? if not how do you use it ?
A. No. we cannot make an object from an Interface and Abstract class. But we can instantiate an anonymous class that implements or extends the interface or abstract class without defining a class object.

what is Access Specifier?
A. Keyword to define where we can access variable object etc.

What is OOP ? Define each feature of java OOP.
A. Object oriented Programming.
Inheritance- classes
Polymorphism- way to create method
Encapsulation- data hiding
Abstraction-implementation hiding

What is Java Package and which package is imported by default?
A.Java compiler imports java.lang package internally by default.

What is API? Name list of API that you have used so far.
A. Application Programming Interface. Java API.

Does java support multiple inheritance, explain why or why not?
A. Java does not support multiple inheritance because in java, every class is a child of Object class.

What is method overloading and when it happens?
A. Method overloading, multiple methods can have the same name with different parameters.

Explain exceptions in java and how to handle it.
A. try and catch method

What is static keyword in java? How it has been used in variables and methods ?
A. The static keyword in Java is used to share the same variable or method of a given class. The users can apply static keywords with variables, methods, blocks, and nested classes. The static keyword belongs to the class than an instance of the class.

What is final and how it has been used variables and methods?
A. The value of final cannot be changed

What is final, finally and finalize?
A. Final is an access modifier, finally is the block in Exception Handling and finalize is the method of object class.

What is a constructor ?
A. It has the same name as class

Can we have multiple constructors in a class?
A. A class can have multiple constructors parameters should be different.

If we don't have a constructor declared, what is called during the object creation?
A. Default constructor

What is "this" keyword in java ?
A.This keyword refers to the current object in a method or constructor

What is "super" keyword in java? How many possible way can you use?
A. The super keyword in java is a reference variable that is used to refer parent class objects.

What is JVM stand for ?
A. Java virtual Machine

Is JVM platform independent?
A. Yes

What version of java are you using?
A. Version 8

What is JAR stand for ?
A. JAR stands for Java ARchive.

What is the difference between JDK and JVM?
A. JDK is Java development Kit and JVM is Java virtual Machine.

What is the difference between JVM and JRE?
A.JVM is Java virtual Machine and JRE is java runtime environment.

What is compile time and run time?
A. Compile time is the period when the programming code is converted to the machine code. Runtime is the period of time when a program is running and generally occurs after compile time.

What is heap?
A. Heap is a dynamic memory

How java manage it's memory?
A. garbage collector







